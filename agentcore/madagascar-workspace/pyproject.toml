[project]
name = "madagascar-workspace"
version = "1.36.1"
description = "Madagascar Workspace - Docker and container-based workspace implementations"

requires-python = ">=3.12"
dependencies = [
    "madagascar-sdk",
    "madagascar-agent-server",
    "pydantic>=2.11.7",
]

[project.urls]
Source = "https://github.com/Madagascar/software-agent-sdk"
Homepage = "https://github.com/Madagascar/software-agent-sdk"
Documentation = "https://docs.madagascar.dev/sdk"
"Bug Tracker" = "https://github.com/Madagascar/software-agent-sdk/issues"

[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[tool.setuptools.package-dir]
"" = "."

[tool.setuptools.packages.find]
include = ["madagascar.workspace*"]
namespaces = true

[tool.setuptools.package-data]
"*" = ["py.typed"]
